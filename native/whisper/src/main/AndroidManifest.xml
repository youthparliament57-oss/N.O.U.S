<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- :native module — empty manifest. The .so files are bundled via
         externalNativeBuild in build.gradle.kts and consumed by :feature:voice. -->
</manifest>
